# dbms-final-project
Contains the database management system (DBMS) final project about the creation of a Budget Management Website using python as the backend as well as sqlite for the database itself.
